import 'package:flutter/material.dart';

class AppColors {
  // Base Palette
  static const bg = Color(0xFF1A1A1A);       // Matte Charcoal
  static const surface = Color(0xFF262626);  // Lighter Charcoal
  static const primary = Color(0xFF4D5D53);  // Tactical Olive
  static const primaryDim = Color(0xFF2F3A33); // Darker Olive for backgrounds
  
  // Status Colors
  static const accent = Color(0xFF22C55E);   // Signal Green (Connected)
  static const alert = Color(0xFFFF4500);    // Warning Orange (Disconnected)
  
  // Text & Borders
  static const text = Color(0xFFFAFAFA);     // Sharp White
  static const textDim = Color(0xFF999999);  // Muted Text
  static const border = Color(0xFF404040);   // Dividers
}